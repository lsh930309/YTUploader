# OAuth Credentials

이 디렉터리는 저장소용 플레이스홀더입니다. 실제 실행 시 앱은 `%LOCALAPPDATA%\\YTUploader\\credentials\\`를 사용합니다.

필요 파일:

- `client_secrets.json`: Google Cloud Console에서 발급한 Desktop OAuth client
- `token.json`: 최초 로그인 후 앱이 생성하는 사용자 토큰

민감 정보 유출 방지를 위해 실제 시크릿 파일은 커밋하지 않습니다.

